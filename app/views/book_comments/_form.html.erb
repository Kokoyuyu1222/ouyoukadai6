<%= form_with(model: [book, comment] ) do |form| %>
  <div>
    <%= form.text_area :comment ,rows:'5', class: "form-control",placeholder: "コメントをここに"%>
  </div>
  <div class="actions">
    <%= form.submit "コメントをする" %>
  </div>
<% end %>